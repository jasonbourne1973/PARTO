PySide6>=6.5.0
matplotlib>=3.7.0
openpyxl>=3.1.0
jdatetime>=4.1.0
Pillow>=10.0.0
reportlab>=4.0.0
arabic-reshaper>=2.1.0
python-bidi>=0.4.0
# مستقیماً در ۸ ماژول (نمودارها و صفحات) import می‌شود؛
# قبلاً فقط به‌صورت غیرمستقیم از طریق matplotlib نصب می‌شد
numpy>=1.24.0

# اجرای تست‌ها: بعد از نصب همین فایل، «python -m pytest» باید بدون
# خطای Import شروع شود و نتیجهٔ واقعی تست‌ها را بدهد (بازرسی دوازدهم)
pytest>=7.0.0

# ابزار lint پروژه (ruff.toml در ریشه). اسکریپت‌های راستی‌آزمایی
# (verify_fixes8/9/10) خودشان «ruff check .» را اجرا می‌کنند؛ بدون نصب
# این بسته، آن بررسی‌ها با پیام «No such file or directory: 'ruff'»
# رد می‌شدند و به‌جای نتیجهٔ واقعیِ lint، خطای محیط گزارش می‌شد.
ruff>=0.6.0

# ===== اختیاری (بازرسی هفتم) =====
# تشخیص دقیق نوع فایل از روی محتوا. اجباری نیست:
#   • اگر نصب باشد، utils/file_validator.py از آن استفاده می‌کند
#   • اگر نباشد، همان فایل با تشخیص امضای محتوا (خالص پایتون) کار می‌کند
# توجه: روی ویندوز به نصب کتابخانهٔ libmagic سیستم نیاز دارد.
# python-magic>=0.4.27
