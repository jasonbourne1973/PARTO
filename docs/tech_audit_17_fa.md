# گزارش دور هفدهم — بازبینی «MASTER FIX / FINAL TECHNICAL AUDIT »

این سند تجمعی است: بخش ۱ (نتیجهٔ بازبینی و مرحله‌بندی) و بخش ۲ به بعد
(اجرای هر مرحله) با پیشرفت کار تکمیل می‌شود. هیچ امتیاز/رتبهٔ کلی به برنامه
داده نمی‌شود.

**شاخهٔ کار:** `arena/01a0ce86-parto` (وارث نسخهٔ اصلاح‌شدهٔ دور شانزدهم)
**مبنای بازبینی:** سند «MASTER FIX / FINAL TECHNICAL AUDIT» (بندهای ۰ تا ۴۵)

**وضعیت محیط اجرا:** این محیط `libGL/libEGL/libxkbcommon/libdbus` سیستم را
ندارد (شبکهٔ apt بسته است). برای اجرای واقعی Qt در حالت `offscreen` سه
کتابخانهٔ جانشین در بیرون مخزن ساخته شده و با
`LD_LIBRARY_PATH=/home/user/qtstub/lib QT_QPA_PLATFORM=offscreen` بارگذاری
می‌شوند. یعنی برخلاف دور شانزدهم، **کد واقعی صفحه‌ها و پنجرهٔ اصلی در همین
محیط اجرا و آزموده می‌شود**؛ ولی کلیک واقعی موس، دیالوگ modal،
`QFileDialog`، بازکردن فایل با برنامهٔ خارجی و اجرای دوبارهٔ فرایند همچنان
`NOT_TESTED - GUI EXECUTION REQUIRED` هستند.

---

# بخش ۱ — بازبینی بند به بند و مرحله‌بندی

## ۱-۱. کلید وضعیت

| نماد | معنا |
|---|---|
| ✅ | در کد پیاده است **و** آزمون خودکار (pytest/verify) دارد و در همین محیط سبز است |
| 🟡 | در کد پیاده است، ولی آزمون/پوشش رگرسیون بند مربوطه ناقص است |
| 🔴 | باقی‌مانده — نیاز به اصلاح کد |
| ⚪ | تصمیم طراحی لازم است (Agent حق حدس سیاست ندارد) |
| 🖥 | فقط با اجرای واقعی GUI قابل اثبات است |

## ۱-۲. جدول وضعیت

| بند | عنوان | وضعیت | شاهد/یادداشت |
|---|---|---|---|
| ۲ | BUG-GUI-02 اطلاعات خانوادگی دانش‌آموز | ✅ | مرحلهٔ ۳ دور قبل: `family_contexts` مرجع قانونی؛ `tests/test_dal.py::TestFamilyContextDAL` + `verify_fixes16 §M` |
| ۳ | BUG-RESTORE-01 مسیر UI بازیابی دانش‌آموز | 🔴 | `dal/student_dal.restore` هست؛ `StudentService` متد restore **ندارد** و `views/pages/students_page.py` هیچ مسیر بازیابی/نمایش حذف‌شده‌ها ندارد |
| ۳ | BUG-RESTORE-02 مسیر UI بازیابی کاربر | 🟡 | `views/pages/settings_page.py::restore_user` + `UserDAL.restore` موجود است؛ آزمون یکپارچه ندارد |
| ۳ | BUG-RESTORE-04/10 حفظ پروندهٔ تاریخی در restore+year+edit | 🟡 | کد مسیرها profile-aware است؛ آزمون یکپارچهٔ IT-RESTORE-YEAR-01 وجود ندارد |
| ۴ | BUG-RESTORE-05/06/07 بازیابی فعالیت/هدف/مشاوره | 🔴 | `restore()` فقط در DAL است (`dal/extracurricular_dal.py:236`, `dal/goal_dal.py:290`, `dal/counseling_session_dal.py:292`)، هیچ متد service و هیچ مسیر UI وجود ندارد. قرارداد `rowcount > 0` برقرار است و باید بماند |
| ۵ | BUG-GUI-12 / NAV-04/05/06 همگام‌سازی سال | ✅ | مرحلهٔ ۴ دور قبل: `views/pages/year_sync.py` + `MainWindow.set_active_year`؛ `tests/test_year_sync.py` + `verify16 §N` |
| ۶ | BUG-GUI-13 هندلر کامبوی سال در شاخص‌ها | 🔴 | `views/pages/indicators_page.py:106` سیگنال سال را به `on_teacher_changed` می‌دهد؛ این هندلر فقط فهرست دانش‌آموزان را می‌سازد و **پنل شاخص‌های انتخاب‌شدهٔ قبلی را پاک/تازه نمی‌کند** (نمایش کهنه). تحلیل و گزارش‌ها هندلر اختصاصی دارند |
| ۷ | BUG-GUI-07/08 جداسازی پایه/پروندهٔ تاریخی | 🔴 | در `indicators_page.load_students_for_teacher` و `analysis_page` فهرست دانش‌آموزان با `profile_dal.get_active_by_students` ساخته می‌شود؛ یعنی **پایهٔ نمایش‌داده‌شده از پروندهٔ فعال می‌آید حتی وقتی سال انتخاب‌شده چیز دیگری است** (نشتی بین سال‌ها). خواندن شاخص‌ها/تحلیل خود درست است |
| ۸ | BUG-NAV-02 مجوز مشاوره/فعالیت/اهداف | ⚪ | هیچ سند سیاست دسترسی برای این سه بخش در مخزن نیست؛ در `views/main_window.py` هم `required=None` است. طبق بند ۸ مأموریت، Agent حق حدس ندارد → ثبت به‌عنوان `DESIGN DECISION REQUIRED` |
| ۹ | BUG-NAV-03 سخت‌سازی مجوز در backend | 🔴 | `SessionManager.has_permission` هیچ مصرف‌کنندهٔ واقعی ندارد؛ سرویس‌ها (کاربر، پشتیبان، حذف، سال تحصیلی، تغییر نقش) هیچ مرز مجوزی ندارند. فقط UI چک می‌کند |
| ۱۰ | BUG-BACKUP-01 شکست checksum = شکست بازیابی | ✅ | `utils/backup.py::restore_backup` پیش از هر تغییر DB برمی‌گردد؛ `verify16 §B` |
| ۱۰ | BUG-BACKUP-03 نبود فایل `.sha256` کنار پشتیبان | ✅ | فقط `logger.warning`؛ نتیجه/پیام کاربر هیچ وضعیت «راستی‌آزمایی‌نشده» ندارد → موفقیت بی‌هشدار برای پشتیبان قدیمی |
| ۱۰ | BUG-BACKUP-04/05 اختلاف متادیتا با فایل‌های فیزیکی | ✅ | `metadata['attachments_count']` فقط شمارش فیزیکی است؛ «تعداد موردانتظار طبق DB»، «فایل‌های بسته‌بندی‌شده» و «فایل‌های گم‌شده» گزارش نمی‌شود و پشتیبان ناقص «کامل» معرفی می‌شود |
| ۱۰ | BUG-BACKUP-07 باقی‌ماندن فایل ایمنی restore | ✅ | پاک‌سازی در `finally` با گارد وجود DB فعال؛ `verify16 §B7/B8` |
| ۱۰ | BUG-BACKUP-09 توقف نخ پشتیبان‌گیری خودکار | ✅ | دستگیرهٔ `AutoBackupHandle` داخل `SettingsPage` نگه داشته می‌شود ولی `MainWindow.closeEvent` (و مسیر logout → `subprocess.Popen`) هیچ‌وقت آن را متوقف نمی‌کند؛ نخ/کارگر ممکن است بعد از «خروج» همچنان روی DB کار کند |
| ۱۱ | BUG-ATT-04 مسیر UI بازیابی پیوست حذف‌شده | 🔴 | `attachment_dialog.py` فقط نمایش/آپلود/دانلود/حذف دارد؛ حذف منطقی است ولی مسیر بازیابی وجود ندارد |
| ۱۱ | BUG-ATT-05 بررسی فایل فیزیکی هنگام بازیابی | 🔴 | تابع بازیابی وجود ندارد؛ در نتیجه بررسی وجود فایل هم نیست (بازیابی نباید موفقیت خاموش بدهد) |
| ۱۱ | BUG-ATT-06 حد ۲۰ فایل | 🟡 | `AttachmentService.MAX_ATTACHMENTS_PER_ENTITY = 20` و بررسی آن پیش از آپلود موجود است؛ آزمون GUI/رگرسیون ۲۰/۲۱ ندارد و اثرش با پیوست‌های بازیابی‌شده سنجیده نشده |
| ۱۲ | اتمیک‌بودن Create/Edit دانش‌آموز | ✅ | `views/dialogs/student_form.py::save_student` سه نوشتن جدا (student → profile → family) با commit مستقل دارد؛ شکست مرحلهٔ ۲ «دانش‌آموز یتیم» می‌سازد |
| ۱۳ | BUG-GUI-06/18/22 پیام موفقیت تکراری | ✅ | `StudentForm.save_student` پیام موفقیت می‌دهد و `students_page.add_student/edit_student` **همان پیام را دوباره** نشان می‌دهد (دو پیام برای یک ذخیره) |
| ۱۴ | لبه‌های ایمپورت دانش‌آموز | 🟡 | تراکنشِ هر ردیف (student+profile) در `utils/excel_importer.py` پیاده شده و آزمون‌های پایه در `verify16 §F` هست؛ سناریوهای «تکراری داخل یک فایل، کد ملی نامعتبر، تلفن نامعتبر، متن یونیکد/Persian، فایل بزرگ، لغو دیالوگ، شکست ساخت پرونده» آزمون‌شده نیستند |
| ۱۵ | BUG-GUI-03 خروجی همهٔ دانش‌آموزان | 🟡 | `export_students = self.all_students or self.students` در کد درست است؛ آزمون رگرسیون چندصفحه‌ای ندارد |
| ۱۶ | رگرسیون Pagination | 🟡 | منطق کد درست است (`current_page` کلمپ، جست‌وجو → صفحهٔ اول، تغییر سال → صفحهٔ اول)؛ آزمون N=0/1/20/21/40/41/100 ندارد |
| ۱۷ | Search/Filter و LIMIT | ✅ | فیلتر سال پیش از `LIMIT` در `ObservationDAL`/`InterventionDAL`/`FollowUpDAL` (و مسیرهای `search`) هست؛ آزمون «>۱۰۰ رکورد در دو سال» ندارد |
| ۱۸ | گزارش‌ها | ✅ | `ReportGenerator.export_to_pdf/export_to_excel` موجود و `verify16 §I` خروجی واقعی کلاس/معلم/عملکرد را می‌سازد؛ رگرسیون شش‌گزارشی کامل ندارد |
| ۱۹ | خروجی AI | ✅ | شش قالب در `views/dialogs/export_ai_dialog.py`، پیگیری‌ها در PDF/Excel/CSV/TXT، شکست Excel → خطا (نه CSV با پسوند xlsx)؛ `verify16 §D` |
| ۲۰ | دامنهٔ سال داشبورد | ✅ | `get_dashboard_data` با سال مؤثر؛ `verify16 §I` |
| ۲۱ | ارتقاء/تکرار پایه | ✅ | `views/pages/promotion_page.py:repeat_grade_students` حفظ پروندهٔ قدیمی؛ `verify16 §I` |
| ۲۲ | یکپارچگی ویرایش رکوردها | ✅ | قاعدهٔ «CREATE با سال انتخاب‌شده / EDIT با پروندهٔ رکورد» در مسیرهای مشاهده/مداخله/پیگیری/فعالیت/هدف/مشاوره پیاده است؛ آزمون رگرسیون یک‌به‌یک ندارد |
| ۲۳ | پیشرفت هدف | ✅ | `progress_percent` کلمپ‌شده در service/DAL/model؛ `verify16 §D` |
| ۲۴ | پیشنهاد → مداخله | ✅ | `verify16 §L` (پیش‌پرکردن + پیوند + شکست پیوند) |
| ۲۵ | خروج خودکار | ✅ | `logout(confirm=False)` + ثبت Audit یک‌بار؛ `verify16 §I` |
| ۲۶ | BUG-GUI-01 کنتراست نام/آیکون کاربر | 🟡 | رنگ‌ها روی کاغذ کنتراست کافی دارند (`#66BB6A` روی `#0B2E4F` ≈ ‎۵٫۲:۱؛ `#F4C542` روی `#0B2E4F` ≈ ‎۸٫۷:۱) ولی آزمون محاسبهٔ کنتراست برای هدر و نمایش واقعی ندارد |
| ۲۷ | تغییر غیرمجاز سال تحصیلی | ✅ | `verify16 §H` (برگشت کامبو + هشدار + دست‌نخوردن DB) |
| ۲۸ | کد مرده | ✅ | حذف‌شده و `verify16 §G` عدم بازگشت را می‌سنجد |
| ۲۹ | Migration | ✅ | `verify16 §A` و `§K` (کشف پویا، تراکنش هر گام، شکست وسط کار، ترمیم) |
| ۳۰ | Constraintهای دیتابیس | 🟡 | دیتابیس تازه آزموده شده (`verify16 §I`)؛ دیتابیس ارتقاءیافتهٔ قدیمی آزمون ندارد |
| ۳۱ | BUG-NAV-07 وابستگی ناوبری به ایندکس | 🔴 | ایندکس‌ها در `views/main_window.py` هم در فهرست منو (۹..۱۳) و هم در `setCurrentIndex(2)`/`setCurrentIndex(5)` دستی نوشته شده‌اند؛ هیچ نگاشت مرکزی/assert وجود ندارد |
| ۳۲ | BUG-NAV-04 reload بازتابی | ✅ | مرحلهٔ ۴ دور قبل (حذف کامل `inspect`) |
| ۳۳ | ایمنی خروج/راه‌اندازی مجدد | ✅ | ثبت Audit یک‌بار + توقف تایمر بی‌کاری + توقف واقعی کارگرها/زمان‌بند پشتیبان پیش از خروج (مرحلهٔ ۱) |
| ۳۴ | مدیریت استثنا | ✅ | سه مورد DAL با لاگ همراه شد؛ صفر مورد در لایه‌های بحرانی (نگهبان `verify17 §B7`) |
| ۳۵ | ممیزی SQL | ✅ | `verify16 §F` (پارامتری‌بودن، بدون رشته‌چسبانی، نبود LIMIT نادرست) |
| ۳۶ | ایمنی پیوست | ✅ | `verify16 §E` (`.part`، `os.replace`، پیوند نمادین، پیمایش مسیر، قرنطینه پیش از حذف) |
| ۳۷ | ایمنی پشتیبان | ✅ | `verify16 §B` (Online Backup API، integrity، ZIP traversal، فایل ایمنی) |
| ۳۸ | IT-RESTORE-YEAR-01 | 🔴 | آزمون یکپارچهٔ «حذف → بازیابی → تغییر سال → ویرایش» وجود ندارد |
| ۳۹ | IT-YEAR-CRUD-01 | 🔴 | آزمون یکپارچهٔ «A در ۱۴۰۳، B در ۱۴۰۴، حذف/بازیابی/ویرایش» وجود ندارد |
| ۴۰ | ماتریس GUI | 🖥 | چک‌لیست در `docs/gui_test_checklist_fa.md`؛ در این محیط همه به‌صورت offscreen + صدا زدن handler اجرا می‌شود، کلیک واقعی نه |
| ۴۱ | معیار پذیرش | 🟡 | پس از پایان مرحله‌ها در بخش نهایی گزارش می‌شود |
| ۴۲ | فهرست «دوباره حل نکن» | ✅ | هیچ‌کدام از آن موارد در این دور بازطراحی نمی‌شود؛ فقط آزمون رگرسیون تقویت می‌شود |
| ۴۳ | گزارش نهایی machine-readable | 🔴 | در پایان تولید می‌شود (`docs/master_fix_report_fa.md` + JSON) |

## ۱-۳. جمع‌بندی بازبینی

| وضعیت | تعداد بند |
|---|---|
| ✅ انجام‌شده و آزمون‌شده | ۲۵ (پس از مرحله‌های ۱ و ۲) |
| 🟡 انجام‌شده با پوشش ناقص | ۹ |
| 🔴 باقی‌مانده (نیاز به کد) | ۹ |
| ⚪ تصمیم طراحی لازم | ۱ |
| 🖥 فقط GUI واقعی | ۱ |

**نکتهٔ مهم دربارهٔ آنچه «تمام‌شده» شمرده شد:** بندهای ۲ و ۵ در همین
نسخه در مرحلهٔ ۳ و ۴ دور جاری اصلاح شده‌اند (اطلاعات خانوادگی و
همگام‌سازی سال) و این بازبینی آن‌ها را دوباره باز نمی‌کند؛ فقط رگرسیون
می‌گیرد.

---

# بخش ۲ — مرحله‌بندی اصلاحات (۸ مرحله)

| مرحله | اولویت | محتوا | بندهای مأموریت |
|---|---|---|---|
| ۱ | P0 | داده و صداقت پشتیبان/بازیابی: وضعیت شفاف checksum، حسابداری واقعی پیوست‌ها در متادیتا، پشتیبان ناقص ≠ کامل، توقف قطعی نخ پشتیبان خودکار در خروج/راه‌اندازی مجدد | ۱۰ (BACKUP-01/03/04/05/07/09)، ۳۳ (بخش کارگرها)، ۳۷ |
| ۲ | P0/P1 | یکپارچگی نوشتن دانش‌آموز: اتمیک‌کردن Create/Edit (student+profile+family) با rollback صریح، یک پیام موفقیت و یک refresh، پاک‌سازی استثناهای بی‌صدا در مسیرهای بحرانی | ۱۲، ۱۳، ۳۴ |
| ۳ | P1 | مسیرهای Restore: بازیابی دانش‌آموز/فعالیت/هدف/مشاوره در service+UI، بررسی نتیجهٔ واقعی، حفظ پروندهٔ تاریخی در زنجیرهٔ restore+year+edit، آزمون‌های یکپارچهٔ IT-RESTORE-YEAR-01 و IT-YEAR-CRUD-01 | ۳، ۴، ۲۲، ۳۸، ۳۹ |
| ۴ | P1 | درستی ایمپورت/خروجی/صفحه‌بندی/جست‌وجو: لبه‌های ایمپورت اکسل، خروجی کامل (نه صفحهٔ جاری)، ماتریس Pagination، فیلتر سال پیش از LIMIT با >۱۰۰ رکورد | ۱۴، ۱۵، ۱۶، ۱۷ |
| ۵ | P2 | امنیت: مرز مجوز واقعی در backend برای عملیات حساس (کاربران، بازیابی پشتیبان، حذف، سال تحصیلی، نقش) + ثبت `DESIGN DECISION REQUIRED` برای سیاست مجوز مشاوره/فعالیت/اهداف + رگرسیون تغییر غیرمجاز سال | ۸، ۹، ۲۷ |
| ۶ | P2 | پیوست‌ها: مسیر بازیابی پیوست حذف‌شده، بررسی وجود فایل فیزیکی، رفتار دقیق حد ۲۰ فایل، و رگرسیون سناریوهای واقعی | ۱۱، ۳۶ |
| ۷ | P2 | سال و UI: هندلر صریح کامبوی سال شاخص‌ها (و وضعیت کهنه)، پایه/پروندهٔ فهرست‌ها از سال انتخاب‌شده، کنتراست هدر با آزمون محاسباتی، نگاشت مرکزی ناوبری، ایمنی خروج/راه‌اندازی مجدد | ۶، ۷، ۲۶، ۳۱، ۳۳ |
| ۸ | P3 | باتری رگرسیون نهایی + گزارش: گزارش‌ها، خروجی AI، داشبورد، ارتقاء/تکرار پایه، پیشرفت هدف، migration، constraints دیتابیس ارتقاءیافته، ماتریس GUI و گزارش پایانی machine-readable | ۱۸–۲۵، ۲۹، ۳۰، ۴۰، ۴۱، ۴۳ |

**تعداد کل: ۸ مرحله.** موارد بند ۲ هفتم (فهرست «دوباره حل نکن») در همهٔ
مرحله‌ها فقط رگرسیون می‌گیرند.

---

# بخش ۳ — مرحلهٔ ۱ (P0): داده و صداقت پشتیبان/بازیابی

## ۳-۱. مواردی که در این مرحله اصلاح می‌شوند

| مورد | وضعیت پیش از اصلاح | اصلاح |
|---|---|---|
| BUG-BACKUP-01 | ✅ بود؛ بدون تغییر می‌ماند | رگرسیون در `verify_fixes17 §A` |
| BUG-BACKUP-03 | فقط `logger.warning` | وضعیت صریح در نتیجه و پیام: `checksum_state` ∈ {`verified`, `legacy_no_sidecar`, `failed`} + هشدار دیده‌شدنی در پیام بازیابی |
| BUG-BACKUP-04/05 | فقط شمارش فیزیکی | حساب‌داری واقعی: «موردانتظار طبق DB»، «بسته‌بندی‌شده»، «گم‌شده» در متادیتا و نتیجه؛ پشتیبان ناقص با ⚠️ معرفی می‌شود و `complete=False` می‌گیرد |
| BUG-BACKUP-07 | ✅ بود؛ بدون تغییر می‌ماند | رگرسیون در `verify_fixes17 §A` |
| BUG-BACKUP-09 | دستگیرهٔ نخ خودکار در خروج متوقف نمی‌شد | `SettingsPage.shutdown_backup_workers()` + `BackupPage.shutdown()` + فراخوانی صریح در `MainWindow.closeEvent` (پیش از `subprocess.Popen` مسیر logout) |

## ۳-۲. الگوی «مسیر کامل» این مرحله

```text
نام قابلیت:   ساخت/بازیابی پشتیبان با گزارش صادقانهٔ وضعیت
صفحه/Widget:  views/pages/backup_page.py (تب پشتیبان‌گیری) + views/pages/settings_page.py (پشتیبان خودکار)
دکمه/Event:   «ایجاد پشتیبان» / «بازیابی» / «بازیابی از فایل» / «توقف پشتیبان خودکار» / بستن برنامه
Handler:      BackupPage.create_backup/restore_backup/restore_from_file/delete_backup + SettingsPage.start/stop_auto_backup + MainWindow.closeEvent
Service:      utils/backup.py::BackupManager (این ماژول خودش لایهٔ سرویس backup است)
DAL:          — (db/attachments از طریق sqlite و پوشهٔ پیوست‌ها)
Database:     اسنپ‌شات آنلاین + جدول attachments (شمارش موردانتظار) + دیتابیس بازیابی‌شده
خروجی:        نتیجهٔ ساخت/بازیابی با success، complete، checksum_state و شمار پیوست‌ها؛ هشدار ⚠️ برای پشتیبان ناقص
وضعیت:        VERIFIED (پس از اصلاح — بخش ۳-۳)
```

## ۳-۳. تغییرات کد

| فایل | تغییر |
|---|---|
| `utils/backup.py` | `attachment_ledger()` و `verify_attachment_ledger_for_restored_db()` (سه عدد مستقل: موردانتظار طبق DB، بسته‌بندی‌شده، گم‌شده)؛ متادیتای پشتیبان و نتیجهٔ `create_backup` شامل همان سه عدد + `complete`؛ پیام ⚠️ برای پشتیبان ناقص؛ `checksum_state`/`checksum_verified` در همهٔ مسیرهای `restore_backup`؛ یادداشت هشدار `_unverified_checksum_note` برای پشتیبان قدیمی؛ گزارش پیوست‌های گم‌شده پس از بازیابی؛ برگشت دیتابیس حتی وقتی خودِ «راستی‌آزمایی پس از جایگزینی» استثنا بدهد |
| `views/pages/backup_page.py` | `_checksum_warning()` (هشدار پیش از تأیید)؛ بررسی وضعیت فایل انتخاب‌شده در `restore_from_file` و رد پشتیبان خراب؛ نمایش هشدار در متن تأییدیهٔ بازیابی؛ `shutdown(timeout)` با انتظار واقعی برای پایان کارگر و بازگشت وضعیت صادقانه؛ استفاده از لاگر ماژول |
| `views/pages/settings_page.py` | `shutdown_backup_workers(timeout)`: توقف واقعی `AutoBackupHandle` + `BackupPage.shutdown` **بدون** تغییر QSettings (انتخاب کاربر برای اجرای بعدی حفظ می‌شود) |
| `views/main_window.py` | `_shutdown_background_workers()` (فهرست صریح، بدون بازتاب) + فراخوانی آن به‌عنوان نخستین کار در `closeEvent` (پیش از `subprocess.Popen` مسیر logout) |
| `verify_fixes17.py` (جدید) | ۱۲ بررسی کارکردی با پیش/پس‌شرط برای مرحلهٔ ۱ |
| `tests/test_backup_ledger.py` (جدید) | ۸ تست واحد برای حساب‌داری پیوست‌ها و وضعیت checksum (کاملاً مستقل از دیتابیس کاربر) |

## ۳-۴. شواهد آزمون (اجرا شده در همین محیط)

```bash
# محیط Qt در این سندباکس بدون libGL نصب می‌شود؛ کتابخانه‌های جانشین بیرون مخزن:
LD_LIBRARY_PATH=/home/user/qtstub/lib QT_QPA_PLATFORM=offscreen \
    .venv/bin/python verify_fixes17.py        # ۱۲ موفق / ۰ ناموفق
    .venv/bin/python verify_fixes16.py        # ۱۰۱ موفق / ۰ ناموفق (رگرسیون دور ۱۶)
    .venv/bin/python -m pytest -q tests       # ۵۴ passed (۴۶ + ۸ تازه)
.venv/bin/ruff check .                        # All checks passed
```

| بررسی | پیش/پس‌شرط | نتیجه |
|---|---|---|
| A۱ ناقص‌بودن پشتیبان | متادیتا داخل ZIP + نتیجهٔ `create_backup` | ✅ `expected=2`, `packaged=1`, `missing=1`, `complete=False`, پیام با ⚠️ |
| A۲ پشتیبان کامل | همان | ✅ `expected=packaged=1`, `missing=0`, `complete=True` بدون ⚠️ |
| A۳ شکست checksum | شمار دانش‌آموزان پیش/پس + فهرست فایل‌ها | ✅ `success=False`, `checksum_state='failed'`، DB و فایل‌ها دست‌نخورده |
| A۴ پشتیبان قدیمی | پیام کاربر | ✅ `checksum_state='legacy_no_sidecar'`, `checksum_verified=False`, پیام «راستی‌آزمایی نشد» |
| A۵ پشتیبان سالم | نتیجه | ✅ `checksum_state='verified'`, `checksum_verified=True` |
| A۶ بازیابی ناقص | شمار پیوست‌های گم‌شده پس از بازیابی | ✅ `attachments_missing_count>=1`, `complete=False`, پیام با ⚠️ |
| A۷ استثنا وسط بازیابی | وجود `partow.db.restore_safety`/`temp_restore` + شمار دانش‌آموزان | ✅ `success=False`، برگشت کامل، بدون باقی‌مانده |
| A۸ شکست کپی پیوست | محتوای فایل پیوست قبلی | ✅ DB بازیابی شد، پیوست قبلی سالم، `attachments_restored=False` با ⚠️ |
| A۹ حفظ قراردادها | sidecar/SHA-256 و وضعیت‌های چهارگانه | ✅ بدون تغییر |
| A۱۰ UI | مسیر واقعی `restore_from_file`/`restore_backup` با QMessageBox شبیه‌سازی‌شده | ✅ رد پشتیبان خراب، هشدار در متن تأییدیه، `shutdown=True` |
| A۱۱ توقف نخ خودکار | `AutoBackupHandle.is_running()` + صدا زدن مسیر خروج پنجرهٔ واقعی | ✅ نخ واقعاً تمام شد، `closeEvent` همان متد را صدا زد |
| A۱۲ نگهبان تنظیمات | سورس `shutdown_backup_workers` در برابر `stop_auto_backup` | ✅ خروج، انتخاب کاربر را خاموش نمی‌کند |

**پوشش بندهای مأموریت در این مرحله:**
BUG-BACKUP-01 ✅ (رگرسیون) · BUG-BACKUP-03 ✅ · BUG-BACKUP-04/05 ✅ ·
BUG-BACKUP-07 ✅ (به‌همراه مسیر استثنا) · BUG-BACKUP-09 ✅ · بند ۳۳ (بخش
کارگرها) ✅ · بند ۳۷ (بدون تضعیف ایمنی پشتیبان) ✅

**آنچه هنوز GUI واقعی می‌خواهد (در این مرحله):**
کلیک واقعی «ایجاد پشتیبان/بازیابی» در پنجره، انتخاب فایل با `QFileDialog`،
اجرای دوبارهٔ واقعی فرایند پس از logout → `NOT_TESTED - GUI EXECUTION REQUIRED`.

---

# بخش ۴ — مرحلهٔ ۲ (P0/P1): یکپارچگی نوشتن دانش‌آموز و استثناهای بی‌صدا

## ۴-۱. موارد این مرحله

| مورد | وضعیت پیش از اصلاح | اصلاح |
|---|---|---|
| بند ۱۲ — اتمیک‌بودن Create/Edit | سه نوشتن با commit مستقل (`student` → `profile` → `family`)؛ شکست مرحلهٔ ۲ یا ۳ «دانش‌آموز یتیم» باقی می‌گذاشت. اثبات معکوس: با کد قبلی یک دانش‌آموز با `profiles=1, family=0` ماند | یک تراکنش واقعی دور هر سه نوشتن؛ شکست ⇒ `rollback` کامل و برگشت مدل در حالت «ایجاد» |
| بند ۱۳ — پیام موفقیت تکراری | فرم پیام «ثبت/ویرایش شد» می‌داد و `students_page.add_student/edit_student` **همان** پیام را دوباره نشان می‌داد (و در مسیر ایجاد، تازه‌سازی هم دو بار معنا داشت) | پیام فقط در «یک» لایه (فرم)؛ صفحه پس از `Accepted` فقط یک بار فهرست را تازه می‌کند |
| بند ۱۳ — نتیجهٔ update | `StudentDAL.update` و `StudentAcademicProfileDAL.update` نتیجهٔ `UPDATE` را بررسی نمی‌کردند؛ به‌روزرسانی رکورد ناموجود/حذف‌شده «موفقیت» برمی‌گرداند | بررسی `rowcount`؛ در نبود اثر: هشدار در لاگ + بازگشت `None`؛ در سرویس دانش‌آموز تبدیل به `ServiceError` با پیام فارسی روشن |
| بند ۳۴ — استثناهای بی‌صدا | ۱۰ مورد `except: pass`؛ سه موردش در `dal/` (مسیرهای داده) | موارد DAL با `logger.debug/warning` همراه شدند؛ دو مسیر جدید هم اضافه شد: خطای حذف دانش‌آموز در UI با `logger.error(exc_info=True)`، و نگهبان آزمون که هر «بلع خاموش تازه» در لایه‌های بحرانی را رد می‌کند |

## ۴-۲. تغییرات کد

| فایل | تغییر |
|---|---|
| `views/dialogs/student_form.py` | بازنویسی `save_student` با `db.begin_transaction()/commit_transaction()/rollback_transaction()`؛ تمرکز سه نوشتن در یک تراکنش؛ پیام خطا با `logger.error(exc_info=True)`؛ جلوگیری از رهاشدن فرم با شناسهٔ رکورد ناموجود پس از rollback |
| `views/pages/students_page.py` | حذف پیام موفقیت تکراری از `add_student`/`edit_student` (پیام فقط از فرم)؛ لاگ traceback در خطای بازکردن فرم و خطای حذف |
| `dal/student_dal.py` | `update()`: بررسی `rowcount`، هشدار لاگ و بازگشت `None` در نبود اثر |
| `dal/student_academic_profile_dal.py` | همان بررسی برای پروندهٔ سالانه |
| `services/student_service.py` | تبدیل «هیچ ردیفی تغییر نکرد» به `ServiceError` با پیام روشن |
| `dal/observation_dal.py`, `dal/staff_dal.py`, `dal/user_dal.py` | سه بلع خاموش باقی‌مانده در لایهٔ داده: رفتار عیناً حفظ، فقط `logger.debug` روشن‌کننده اضافه شد |
| `verify_fixes17.py` | بخش B (۹ بررسی) برای این مرحله |
| `tests/test_student_atomicity.py` (جدید) | ۶ تست: rollback هر سه نوشتن، commit هر سه، و نتیجهٔ واقعی `update` در DAL/پرونده |

## ۴-۳. شواهد آزمون

```bash
LD_LIBRARY_PATH=/home/user/qtstub/lib QT_QPA_PLATFORM=offscreen \
    .venv/bin/python verify_fixes17.py     # ۲۱ موفق / ۰ ناموفق (بخش A: ۱۲، بخش B: ۹)
    .venv/bin/python verify_fixes16.py     # ۱۰۱ موفق / ۰ ناموفق (بدون رگرسیون)
    .venv/bin/python -m pytest -q tests    # ۶۰ passed
.venv/bin/ruff check .                     # All checks passed
```

### اثبات معکوس (اجرای واقعی با کد قبلی)

با برگرداندن پنج فایل مرحلهٔ ۲ به نسخهٔ قبل (`git stash push`)، چهار بررسی
بخش B سرخ می‌شوند و گزارش دقیقاً همان آسیب قبلی را نشان می‌دهد:

```text
❌ [B] اتماست: ... student=2 profiles=1 family=0        ← دانش‌آموز یتیم (شکست مرحلهٔ ۳)
❌ [B] اتماست: ... student=3                            ← دانش‌آموز یتیم (شکست مرحلهٔ ۲)
❌ [B] پیام تکراری: msgs=['info', 'info']                ← دو پیام برای یک ذخیره
❌ [B] update: missing=<Student object> deleted=<Student object>  ← موفقیت صوری update
```

**پوشش بندهای مأموریت در این مرحله:** بند ۱۲ ✅ · بند ۱۳ ✅ · بند ۳۴ ✅
(لایه‌های بحرانی) · بند ۴۲ ✅ (مسیرهای قبلاً اصلاح‌شده بازطراحی نشدند؛
`delete/restore` و پروفایل تاریخی فقط آزمون گرفتند).

**آنچه هنوز GUI واقعی می‌خواهد:** کلیک واقعی «ذخیره» در فرم modal، دیالوگ
خطا/موفقیت واقعی → `NOT_TESTED - GUI EXECUTION REQUIRED`.
