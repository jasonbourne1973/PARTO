# چک‌لیست تست GUI واقعی — برای داور/مدیر پروژه

این چک‌لیست همان مسیرهایی است که در `verify_fixes16.py` به‌صورت headless (offscreen)
با پیش/پس‌شرط دیتابیس/فایل تأیید شده‌اند ولی **کلیک واقعی، دیالوگ‌های modal،
file picker و بازکردن فایل با برنامهٔ خارجی** فقط با اجرای واقعی قابل اثبات‌اند
(`NOT_TESTED - GUI EXECUTION REQUIRED`). هر ردیف یک پس‌شرط قابل مشاهده دارد؛
«کلیک شد و خطا نداد» کافی نیست.

## پیش از شروع
```bash
python -m venv venv && source venv/bin/activate      # ویندوز: venv\Scripts\activate
pip install -r requirements.txt
python verify_fixes16.py                              # باید «۹۳ موفق / ۰ ناموفق» بدهد
python main.py                                        # ورود: admin / Admin@123 (تغییر رمز اجباری)
```
(`verify_fixes16.py` روی دیتابیس موقت کار می‌کند و به `database/partow.db` دست نمی‌زند.)

## ۱) ورود و خروج
| # | عمل | پس‌شرط |
|---|---|---|
| 1.1 | رمز اشتباه → ورود | پیام خطا؛ پنجرهٔ اصلی باز نمی‌شود |
| 1.2 | `admin / Admin@123` | دیالوگ «تغییر رمز» (اولین ورود)؛ رمز فعلی اشتباه → خطا؛ رمز جدید ضعیف → رد |
| 1.3 | دکمهٔ «🚪 خروج» | دیالوگ تأیید؛ «بله» → برنامه دوباره باز می‌شود؛ در «تنظیمات → …» یا جدول audit_logs فقط **یک** ردیف `logout` |
| 1.4 | ۳۰ دقیقه بی‌کاری (یا موقتاً `idle_timer.setInterval` کوتاه) | خروج **بدون** دیالوگ تأیید |

## ۲) پروندهٔ دانش‌آموز (دانش‌آموزان → دابل‌کلیک)
| # | عمل | پس‌شرط |
|---|---|---|
| 2.1 | «➕ مشاهدهٔ جدید» فقط با فیلدهای الزامیِ نمایان (بدون «فیلدهای بیشتر») | ثبت می‌شود؛ ردیف در تب مشاهدات؛ **بدون** «توضیحات باید حداقل ۳ کاراکتر» |
| 2.2 | انتخاب شایستگی از درخت (فرزند یک دسته) → ذخیره | «انتخاب‌شده: …» زیر درخت؛ ستون شایستگی در جدول |
| 2.3 | دابل‌کلیک روی دکمهٔ ذخیره در هر فرم | فقط **یک** رکورد ساخته می‌شود |
| 2.4 | ویرایش یک پیگیری | فرم با دانش‌آموز و **مداخلهٔ خودش** باز می‌شود؛ ذخیره بدون «لطفاً یک مداخله انتخاب کنید» |
| 2.5 | «📎 پیوست‌ها» → افزودن **دو** فایل هم‌زمان (یکی `.png`، یکی `.txt`) | یک پیام جمع‌بندی «۲ فایل…»؛ هر دو در فهرست؛ نوار پیشرفت؛ دکمه‌ها دوباره فعال |
| 2.6 | همان دیالوگ: افزودن `.exe` | پیام خطا؛ فهرست بدون تغییر |
| 2.7 | انتخاب فایل متنی → پیش‌نمایش؛ «دانلود»؛ «باز کردن»؛ «حذف» | متن نمایش داده می‌شود؛ فایل ذخیره می‌شود؛ با برنامهٔ پیش‌فرض باز می‌شود؛ حذف → فهرست −۱ |
| 2.8 | تب «💡 پیشنهادها» → «تولید پیشنهادات» | پیشنهادها با اولویت رنگی؛ متن «اقدام پیشنهادی» **خوانا** |
| 2.9 | روی یک پیشنهاد: «رد» (با دلیل) و روی دیگری: «پذیرش → اجرا → تکمیل» (با بازخورد) | دیالوگ ورود متن باز می‌شود (نه خطا)؛ وضعیت‌ها تغییر می‌کند |
| 2.10 | روی یک پیشنهاد: «ثبت مداخله» | فرم مداخله با **نوع/شرح/هدف پیشنهاد** پیش‌پر است؛ پس از ذخیره: پیشنهاد «اجراشده» و «🔗 مداخلهٔ ثبت‌شده… #id»؛ تب مداخلات یک ردیف بیشتر |
| 2.11 | «📄 گزارش پرونده» | صفحهٔ گزارش‌ها با همان دانش‌آموز انتخاب‌شده و گزارش بارگذاری‌شده باز می‌شود |

## ۳) گزارش‌ها
| # | عمل | پس‌شرط |
|---|---|---|
| 3.1 | انتخاب دانش‌آموز | «جهت تغییر» یک جهت واقعی نشان می‌دهد (نه همیشه «دادهٔ کافی نیست») |
| 3.2 | خروجی PDF و Excel | فایل انتخاب‌شده ساخته و باز می‌شود؛ محتوا فارسی و خوانا |
| 3.3 | گزارش والدین | بدون متن مشاهدات، غربالگری، تفسیر و شرح مداخله؛ یادداشت حریم در انتها |
| 3.4 | تب گزارش کلاس / گزارش معلم / عملکرد معلم → تولید → PDF/Excel | فایل معتبر؛ در نبود داده: پیام «داده کافی نیست» و **بدون** فایل |
| 3.5 | خروجی AI (۶ قالب) و «کپی پرامپت» | ۶ فایل معتبر؛ متن در کلیپ‌بورد |

## ۴) دانش‌آموزان
| # | عمل | پس‌شرط |
|---|---|---|
| 4.1 | «جست‌وجوی پیشرفته» فقط با نام (تاریخ تولد خالی) | نتیجه می‌آید؛ دابل‌کلیک → پروندهٔ همان دانش‌آموز |
| 4.2 | «دانلود فایل نمونه» → «ایمپورت از اکسل» همان فایل | همهٔ ردیف‌های نمونه وارد می‌شوند (نه «ستون‌های ضروری یافت نشدند») |
| 4.3 | ایمپورت فایل با کد ملی تکراری / پایهٔ غیرعددی / فایل متنی با پسوند xlsx | ردیف‌های خراب گزارش می‌شوند؛ بقیه وارد؛ فایل خراب → پیام روشن بدون crash |
| 4.4 | «خروجی Excel» فهرست | فایل معتبر |

## ۵) ساختار آموزشی / تنظیمات
| # | عمل | پس‌شرط |
|---|---|---|
| 5.1 | ✏️ روی یک کلاس → تغییر نام/ظرفیت → «💾 ذخیرهٔ تغییرات» | ردیف جدول به‌روز؛ فرم به حالت «افزودن» برمی‌گردد؛ «انصراف» فرم را خالی می‌کند |
| 5.2 | تنظیمات → اطلاعات مدرسه → مقادیر واقعی → ذخیره → بستن و بازکردن برنامه | همان مقادیر برمی‌گردند (نه «مدرسهٔ نمونه») |
| 5.3 | تنظیمات → پشتیبان خودکار → شروع (فاصلهٔ کوتاه) → توقف | پس از توقف دیگر فایل `auto_backup_*` ساخته نمی‌شود |
| 5.4 | کامبوی «سال تحصیلی» در هدر → سال دیگر | دیالوگ تأیید؛ «بله» → صفحه‌ها بر اساس سال جدید؛ سال بایگانی‌شده → هشدار و برگشت |

## ۶) پشتیبان‌گیری (تنظیمات → پشتیبان)
| # | عمل | پس‌شرط |
|---|---|---|
| 6.1 | «ایجاد پشتیبان» | فایل `.partobak` + `.sha256` در `backups/` (نه داخل `views/`)؛ ستون وضعیت «✅ سالم»؛ هیچ `.db.tmp-wal` کنارش |
| 6.2 | ویرایش دستی یک بایت از فایل پشتیبان → تازه‌سازی | وضعیت «❌ checksum نامعتبر»؛ فایل غیر-ZIP → «❌ خراب» و دکمهٔ بازیابی غیرفعال |
| 6.3 | ثبت یک دانش‌آموز جدید → بازیابی از پشتیبان قبلی | دانش‌آموز جدید حذف می‌شود؛ پیام موفقیت؛ پیوست‌ها برگشته‌اند |
| 6.4 | «حذف» یک پشتیبان | فایل و `.sha256` هر دو حذف؛ حذف وسط پشتیبان‌گیری → «لطفاً صبر کنید» |
| 6.5 | «بازیابی از فایل» با فایل خراب | پیام خطا؛ دیتابیس دست‌نخورده |

## ۷) داشبورد
| # | عمل | پس‌شرط |
|---|---|---|
| 7.1 | دکمه‌های «🔄 به‌روزرسانی» دو داشبورد | متن دکمه **خوانا**؛ داده تازه می‌شود |
| 7.2 | داشبورد تحلیلی → دابل‌کلیک روی دانش‌آموز «بدون مشاهده» | پروندهٔ همان دانش‌آموز باز می‌شود |
| 7.3 | نشان‌های «مشاهدات ماهانه» / «همه به موقع» / «فعال» | متن خوانا (قبلاً هم‌رنگ زمینه) |
| 7.4 | زنگولهٔ هدر | یادآورهای پیگیری؛ کلیک → صفحهٔ پیگیری‌ها |

## ۸) راه‌اندازی
| # | عمل | پس‌شرط |
|---|---|---|
| 8.1 | خراب‌کردن عمدی `database/partow.db` (مثلاً یک فایل متنی) و اجرای برنامه | **پنجرهٔ خطا** نمایش داده می‌شود (نه بسته‌شدن بی‌صدا)؛ خطا در `logs/` با traceback |
| 8.2 | دیتابیس نسخهٔ قدیمی‌تر (مثلاً از یک پشتیبان قدیمی) | Migration کامل یا خطای روشن؛ `db_version` هرگز جلوتر از اسکیما نیست |
