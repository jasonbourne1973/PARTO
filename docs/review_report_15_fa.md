# گزارش دور پانزدهم — داوری دوم مدیر پروژه (محتوایی + فنی)

این دور در **چند مرحله** انجام می‌شود (به درخواست صریح کاربر). این سند با هر
مرحله تکمیل می‌شود.

- **مرحلهٔ ۱ (این سند، بخش‌های ۱ تا ۵):** سه ایراد محتوایی 🟠 داوری
  (روند با بازه‌های میانی، روایت چندساله با «تغییر پس از مداخله»، گزارش
  والدین با منبع واحد) + یک ایراد واقعی که حین همین کار کشف شد.
- **مرحلهٔ ۲ (بعدی):** باقی‌ماندهٔ فنی که در کد فعلی هنوز قابل بهبود است
  (تریگرهای `notifications` ساختهٔ migration_v6، توضیحات/داک‌استرینگ‌های
  کهنه که کد قدیمی را نقل می‌کنند، `print`های manager.py) + بخش Pylance
  اگر متن آن برسد.

پایهٔ کار: شاخهٔ `arena/01a0bdea-parto`، کامیت `b0d2721` (پایان دور ۱۴).

---

## ۰) جمع‌بندی یک‌نگاهی مرحلهٔ ۱

| # | ایراد داوری | وضعیت در `b0d2721` (پیش از این مرحله) | اقدام این مرحله | فایل‌ها | آزمون |
|---|---|---|---|---|---|
| م۱ | روند: نتیجه نباید عمدتاً از مقایسهٔ اولین/آخرین بازه گرفته شود | روند ماهانه (`growth_direction`) از دور ۱۳ مسیرمحور بود و مثال داوری را درست «غیرقطعی» می‌داد؛ **اما** روند چندسالهٔ `TrendAnalysisService._analyze_multi_year_overall` هنوز فقط `year_data[0]` را با `year_data[-1]` مقایسه می‌کرد و آستانه‌های جداگانهٔ خودش را داشت | مسیر چندساله هم به `growth_direction` روی **همهٔ سال‌ها** منتقل شد (کلیدهای خروجی حفظ؛ `path_text`/`turning_points` اضافه) | `services/trend_analysis_service.py` | verify15 §A (۹ بررسی) |
| م۱-ب | **ایراد واقعی کشف‌شده:** «جهت تغییر» در گزارش سالانهٔ برنامه و PDF همیشه «دادهٔ کافی برای تحلیل روند» بود | `calculate_trend` حجم ماه را با کلید `count` می‌داد ولی `growth_direction` بازه‌ها را با `total` می‌شناسد → همهٔ ماه‌ها «بدون داده» شمرده می‌شدند | `calculate_trend` اکنون `total` هم می‌دهد و `calculate_trend_direction` هر دو کلید را می‌پذیرد | `services/report_generator.py` | verify15 §A (A7، A8) |
| م۲ | روایت چندساله: مسیر رشد قابل فهم؛ «تغییر بعد از مداخله» دیده شود (کمتر شده / ادامه یافته / برطرف شده) | روایت منسجم دور ۱۳ (۵ بخش + جمع‌بندی) وجود داشت ولی وضعیت **همان زمینه در سال بعد از مداخله** صریح نبود | برای هر زمینهٔ نیازمند توجه یا دارای مداخله، **مسیر سال‌به‌سال زمینه** + «تغییر پس از مداخله» ساخته می‌شود و در بخش ۳ روایت و جمع‌بندی بخش ۵ می‌آید (بدون بخش/کلید جدید در ساختار؛ همان رندر مشترک برنامه/PDF/Excel) | `services/report_generator.py` | verify15 §B (۱۰ بررسی) |
| م۳ | گزارش والدین: دو مسیر مستقل → یک منبع واحد؛ اطلاعات داخلی وارد نشود | `reports_page` از `ReportGenerator.generate_parent_report` استفاده می‌کرد؛ `ParentReportService` (با منطق جداگانهٔ قوت/ضعف/روند/پیشنهاد و PDF) هیچ‌جا استفاده نمی‌شد | `ParentReportService.generate_parent_report_data` **تنها منبع** شد و روی همان تحلیل گزارش داخلی ساخته می‌شود؛ صفحه، PDF و متد سازگاری همه از آن می‌خوانند؛ لایه‌های داخلی صریحاً بیرون نگه داشته می‌شوند | `services/parent_report_service.py`, `services/report_generator.py`, `views/pages/reports_page.py` | verify15 §C (۹ بررسی) |
| — | منطق قوت/ضعف («این قسمت را دیگر تغییر ندهید») | — | **دست نخورد** (`utils/behavior_analysis.py` بدون تغییر) | — | verify15 §D |

نتیجهٔ باتری پس از مرحلهٔ ۱: verify_fixes 1–15 = **۵۴۸/۵۴۸**، pytest **۲۶/۲۶**،
`ruff check .` صفر یافته (جدول کامل در بخش ۵).

---

## ۱) یک نکتهٔ مهم دربارهٔ متن داوری فنی

فهرست ۱۵ موردی فنی در این داوری، کلمه‌به‌کلمه همان فهرست دور ۱۴ است و
چند نشانه می‌گوید نسخه‌ای که بررسی شده، **نسخهٔ قبل از دور ۱۴** بوده است:

| نشانه در متن داوری | وضعیت در کد فعلی (`b0d2721`) |
|---|---|
| «نسخهٔ واقعی برنامه 27.2.3» | `APP_VERSION = '27.2.4'` (`config/settings.py`) و همان مقدار در متادیتای پشتیبان ثبت می‌شود (verify14 §D، verify12 §D) |
| «Audit فقط ۷ جدول: students, observations, interventions, followups, student_academic_profiles, staff, competencies» | `_AUDIT_TABLES` = **۱۸ جدول** با ۵ تریگر (insert/update/soft_delete/restore/hard_delete) و JSON کامل ردیف؛ کامنت ۷ جدولی `BaseService` در مرحلهٔ ۲ دور ۱۴ حذف شده |
| «دو DAL برای تفسیر حرفه‌ای (`dal/interpretation_dal.py` و `dal/professional_interpretation_dal.py`)» | `dal/interpretation_dal.py` در `b0d2721` **حذف شده** (`git rm`) |
| «تریگرها فقط `json_object('id', OLD.id)` ذخیره می‌کنند» | تریگرهای ۱۸ جدول، تصویر کامل ردیف را ثبت می‌کنند (`_audit_json_expression`)؛ تنها جایی که این عبارت هنوز وجود دارد `database/migrations/migration_v6.py` (تریگرهای `notifications`) است → **مرحلهٔ ۲** |
| «`migrations = {1: self._migrate_to_v1}` و `_set_db_version(to_version)` پس از شکست» | این عبارت فقط در **داک‌استرینگ** `_migrate_database` (به‌عنوان نقل کد قدیمی) مانده؛ کدِ واقعی پس از شکست `MigrationManager` خطا را بالا می‌فرستد و نسخه مُهر نمی‌شود (verify14 §D2، verify12 §H). داک‌استرینگ گمراه‌کننده در مرحلهٔ ۲ اصلاح می‌شود |
| «اتصال مشترک با `check_same_thread=False`» | اتصال‌ها نخ‌محلی‌اند (دور ۱۲ و ۱۴؛ verify14 §A). پرچم فقط برای این لازم است که `close_all()` هنگام بازیابی/خروج بتواند اتصال نخ‌های دیگر را زیر قفل ببندد؛ توضیح کنار آن در مرحلهٔ ۲ بازنویسی می‌شود |
| «backup: fallback `shutil.copy2` روی دیتابیس زنده»، «`zipf.extractall`» | پشتیبان با `sqlite3.backup` و بدون مسیر جایگزینِ کپی زنده؛ استخراج با `_safe_extract` (verify14 §C). `shutil.copy2` تنها در **مسیر بازیابی** (جایگزینی فایل پس از اعتبارسنجی) و در داک‌استرینگ‌ها (نقل کد قدیمی) دیده می‌شود |
| «`_should_cleanup` همیشه True»، «بررسی تکرار اعلان با `get_by_user(limit=10)`» | پاکسازی واقعی ۲۴ساعته (verify14 §J)؛ بررسی تکرار با `NotificationDAL.exists_for_entity` (verify14 §G) |
| «pytest: ModuleNotFoundError jdatetime» | `requirements.txt` شامل `jdatetime` و `pytest`؛ در محیط تمیزِ ساخته‌شده فقط از همین فایل، pytest 26/26 (verify14 §I) |

بنابراین از ۱۵ + ۱ مورد فنی، آنچه در **کد فعلی** واقعاً باقی مانده و به
مرحلهٔ ۲ می‌رود:

1. تریگرهای `trg_notifications_insert_audit` / `trg_notifications_update_audit`
   که `migration_v6` با `CREATE TRIGGER IF NOT EXISTS` و فقط با `id` ساخته و
   جدول `notifications` در `_AUDIT_TABLES` نیست (همان ایراد «IF NOT EXISTS
   روی دیتابیس‌های موجود»).
2. توضیحات/داک‌استرینگ‌هایی که کد خطرناک قدیمی را نقل می‌کنند و خوانندهٔ
   ایستا (انسان یا ابزار) را گمراه می‌کنند: داک‌استرینگ `_migrate_database`،
   توضیح کنار `check_same_thread=False`، داک‌استرینگ‌های `utils/backup.py`.
3. `print` در `database/migrations/manager.py` برای خطای بارگذاری ماژول
   (مسیر «نسخهٔ گمشده» درست خطا می‌دهد؛ فقط ثبت رخداد باید از logger باشد).
4. بخش «Pylance در VS Code» در متن داوری نیامده است؛ با رسیدن متن، در
   همان مرحله بررسی می‌شود.

---

## ۲) م۱ — روند رشد با لحاظ بازه‌های میانی (همهٔ مسیرها)

### ۲-۱) چه چیزی از قبل درست بود
`utils/behavior_analysis.growth_direction` از دور ۱۳ مسیر بین همهٔ بازه‌ها
را می‌سنجد. مثال دقیق داوری:

```
مهر ۲۰٪/۷۰٪ ← آبان ۶۰٪/۳۰٪ ← آذر ۲۵٪/۶۵٪
→ status = mixed، label = «تغییر ترکیبی (روند غیرقطعی)»، turning_points = ['آبان']
```

### ۲-۲) چه چیزی هنوز اشتباه بود و اصلاح شد
- `TrendAnalysisService._analyze_multi_year_overall` (روند چندساله) هنوز فقط
  سال اول و آخر را مقایسه می‌کرد (`positive_change > 0.15 / 0.05 …`). اکنون
  روی همهٔ سال‌ها `growth_direction` اجرا می‌شود؛ وضعیت‌های `slightly_*`
  حذف و به همان چهار وضعیت مشترک (+ `insufficient`) نگاشت شد. کلیدهای
  قبلی خروجی حفظ شده‌اند و `direction`، `path_text`، `turning_points` اضافه
  شده‌اند. (این متد در حال حاضر فقط از تست‌ها صدا زده می‌شود، ولی منطق سرویس
  باید با اصل داوری هم‌خوان باشد.)
- **ایراد واقعی کشف‌شده:** در گزارش سالانهٔ برنامه (`reports_page` → «جهت
  تغییر») و PDF (`export_to_pdf`)، `calculate_trend_direction(report['trend_data'])`
  همیشه «دادهٔ کافی برای تحلیل روند» برمی‌گرداند، چون `calculate_trend`
  حجم هر ماه را با کلید `count` می‌داد و `growth_direction` بازه‌های بدون
  `total` را «بدون داده» می‌شمرد. اصلاح: `calculate_trend` کلید `total` را هم
  می‌دهد و `calculate_trend_direction` هر دو کلید را می‌پذیرد. اصل «تعداد
  مشاهدات شاخص رشد نیست» در پیام‌ها دست‌نخورده است.

---

## ۳) م۲ — روایت چندساله: مسیر هر زمینه و «تغییر پس از مداخله»

### ۳-۱) طراحی (بدون قابلیت جدید؛ تکمیل روایت موجود)
`generate_growth_narrative` برای هر سال، شمارش هر زمینه (`areas`) را نگه
می‌دارد و `_build_area_paths` برای هر زمینه‌ای که در یکی از سال‌ها «نیازمند
توجه» بوده **یا** مداخله‌ای به آن پیوند خورده (مداخله ← مشاهدهٔ مبنا ←
شایستگی)، مسیر سال‌به‌سال می‌سازد:

- وضعیت زمینه در هر سال: نیازمند توجه / توانمندی / ترکیبی / ثبت کم / مشاهده‌ای
  ثبت نشده — همراه با مداخله‌های همان سال و نتیجهٔ پیگیری‌شان.
- برای هر سالِ دارای مداخله، وضعیت همان زمینه در **سال بعد** با
  `_post_intervention_status` تعیین می‌شود:

| وضعیت | شرط |
|---|---|
| `resolved` — برطرف شده (با احتیاط) | در سال بعد برای زمینه مشاهده هست ولی رفتار منفی ثبت نشده |
| `reduced` — کمتر شده | سهم منفی دست‌کم ۱۰ واحد کم شده، یا زمینه دیگر الگوی منفی نیست |
| `continued` — ادامه یافته | هنوز الگوی منفی است و سهم منفی معنادار کم نشده |
| `increased` — بیشتر شده | سهم منفی دست‌کم ۱۰ واحد زیاد شده |
| `no_data` | در سال بعد مشاهده‌ای برای این زمینه نیست (**«برطرف شده» تلقی نمی‌شود**) |
| `pending` | سال بعدی هنوز ثبت نشده |

خروجی در بخش ۳ روایت («زمینه‌هایی که تغییر کرده‌اند — با مسیر هر زمینه و
تغییر پس از مداخله») و یک خط جمع‌بندی در بخش ۵ می‌آید؛ چون رندر مشترک
(`growth_narrative_lines`) است، گزارش معمول برنامه، PDF و Excel یکسان آن را
نشان می‌دهند. ساختار (کلیدهای بخش‌ها، تعداد سرفصل‌ها، انواع اولویت) تغییر
نکرده تا قراردادهای دور ۱۳ پابرجا بماند. کلیدهای ساخت‌یافتهٔ جدید در
`synthesis`: `area_paths`، `post_intervention_counts`، `post_intervention_summary`.

### ۳-۲) سناریوی داوری (verify15 §B، روی دیتابیس موقت)
```
سال ۱: «احترام» نیازمند توجه (۳ منفی از ۳) + گفتگوی فردی (پیگیری: بهبود مشاهده شد)
سال ۲: «احترام» توانمندی (۱ منفی / ۲ مثبت از ۳) + تماس با والدین (بدون پیگیری)
سال ۳: «احترام» توانمندی (۰ منفی / ۲ مثبت از ۲)
→ پس از مداخلهٔ سال ۱ در سال ۲: کمتر شده (۳ منفی از ۳ ← ۱ منفی از ۳)
→ پس از مداخلهٔ سال ۲ در سال ۳: برطرف شده (رفتار منفی ثبت نشده؛ نیازمند تأیید با مشاهدهٔ بیشتر)
```
همچنین: نیاز ادامه‌دار → «ادامه یافته»؛ مداخله در آخرین سال → «سال بعدی هنوز
ثبت نشده»؛ نبودِ مشاهدهٔ زمینه در سال بعد → «قابل جمع‌بندی نیست» (نه برطرف
شده)؛ دانش‌آموز تک‌سال بدون ادعای بهبود.

---

## ۴) م۳ — گزارش والدین با یک منبع واحد

### ۴-۱) تصمیم
منبع نهایی **`ParentReportService.generate_parent_report_data`** است، ولی
دیگر منطق جداگانه ندارد: تحلیل‌ها یک بار در
`ReportGenerator.generate_student_report` ساخته می‌شوند (همان الگوهای
رفتاری تأییدشده، همان روند ماهانه، همان اثربخشی مداخلات، همان پیشنهادها) و
این سرویس فقط «نمای والدین» می‌سازد. در نتیجه:

- `views/pages/reports_page.show_parent_report` → `ParentReportService` (همان
  سرویسی که PDF والدین را می‌سازد).
- `ReportGenerator.generate_parent_report` فقط برای سازگاری با
  فراخوانی‌های قبلی به همین سرویس واگذار می‌کند (بدون منطق).
- `export_parent_report_pdf` از همان دادهٔ واحد می‌خواند.
- منطق‌های تکراری حذف شدند: استخراج مستقل قوت/ضعف، گروه‌بندی ماهانهٔ
  مستقل روند، پیشنهادهای مستقل. پیشنهادهای والدین همان پیشنهادهای گزارش
  داخلی‌اند که فقط «شناسهٔ مشاهدات» (ردیابی داخلی) از آن‌ها برداشته شده،
  به‌علاوهٔ یادداشت‌های زمینه‌ای مخصوص والدین (حجم ثبت کم، غلبهٔ رفتار
  نیازمند توجه، پیگیری در انتظار) که قبلاً هم در سرویس والدین بود.

### ۴-۲) چه چیزهایی عمداً وارد گزارش والدین نمی‌شود (`PARENT_EXCLUDED_LAYERS`)
متن و شناسهٔ مشاهدات، نمونه‌های رفتار، جزئیات زمینهٔ خانوادگی و متن
مصاحبه‌ها (فقط «وجود/عدم وجود» گزارش می‌شود)، نتایج غربالگری، تفسیر
حرفه‌ای، جلسات مشاوره، شرح/هدف مداخلات. آزمون §C با درج نشانگرهای
`SECRET_*` در این لایه‌ها ثابت می‌کند که در گزارش داخلی هستند ولی در دادهٔ
گزارش والدین نیستند. یک `privacy_note` هم در متن برنامه و PDF می‌آید.

### ۴-۳) سازگاری
کلیدهای قبلی که صفحه استفاده می‌کرد حفظ شده‌اند (`student_name`, `grade`,
`class`, `summary`, `strengths`, `weaknesses`, `recommendations['parents']`,
`trend`, `observations_count`, `interventions_count`, …). کلید `trend` اکنون
دیکشنری روند والدین‌فهم است (قبلاً فهرست خام ماهانه بود) و بخش «روند کلی»
صفحه همان متن روند + یادداشت حجم ثبت را نشان می‌دهد.

---

## ۵) باتری کامل پس از مرحلهٔ ۱

| مجموعه | نتیجه |
|---|---|
| `pytest -q tests` | 26 passed |
| verify_fixes 1 … 14 | 17 / 32 / 50 / 36 / 25 / 34 / 30 / 62 / 38 / 37 / 30 / 43 / 32 / 50 — همه سبز |
| **verify_fixes15 (جدید)** | **32 / 32** |
| جمع | **۵۴۸ / ۵۴۸** |
| `ruff check .` | All checks passed |

اجرای رابط‌گرافیکی صفحهٔ گزارش‌ها (offscreen): `show_parent_report` روی
دادهٔ آزمایشی، متن کامل گزارش والدین را با دادهٔ واحد (قوت/ضعف، پیشنهادها،
روند، یادداشت حریم) می‌سازد.

فایل‌های تغییریافته در مرحلهٔ ۱: `services/trend_analysis_service.py`،
`services/report_generator.py`، `services/parent_report_service.py`،
`views/pages/reports_page.py`، `verify_fixes15.py` (جدید)، `README.md`،
همین سند. `utils/behavior_analysis.py` تغییر نکرده است.
